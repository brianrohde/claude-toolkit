# docs-update-all

**Tier**: beta (proven in CMT_Codebase, not yet reused elsewhere)

## Purpose

>

## Installation

```
cp -r docs-update-all/ <project>/.claude/skills/
```

Then commit to the project's repo.

## Provenance

Ported from `CMT_Codebase/.claude/skills/docs-update-all/` on 2026-04-22.

## Slash-ref rewrites applied during port

- `/draft-commit\b` -> `/git-draft-commit` (5 occurrences)
- `/log-standup\b` -> `/standup-log` (2 occurrences)
- `/update-all-docs\b` -> `/docs-update-all` (7 occurrences)
