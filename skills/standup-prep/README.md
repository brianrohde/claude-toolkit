# standup-prep

**Tier**: beta (proven in CMT_Codebase, not yet reused elsewhere)

## Purpose

|

## Installation

```
cp -r standup-prep/ <project>/.claude/skills/
```

Then commit to the project's repo.

## Provenance

Ported from `CMT_Codebase/.claude/skills/standup-prep/` on 2026-04-22.

## Slash-ref rewrites applied during port

- `/log-standup\b` -> `/standup-log` (1 occurrences)
- `/prep-standup\b` -> `/standup-prep` (5 occurrences)
- `/finalize-standup\b` -> `/standup-finalize` (6 occurrences)
