# test-codebase-integrity

**Tier**: beta (proven in CMT_Codebase, not yet reused elsewhere)

## Purpose

|

## Installation

```
cp -r test-codebase-integrity/ <project>/.claude/skills/
```

Then commit to the project's repo.

## Provenance

Ported from `CMT_Codebase/.claude/skills/test-codebase-integrity/` on 2026-04-22.

## Slash-ref rewrites applied during port

- `/draft-commit\b` -> `/git-draft-commit` (1 occurrences)
- `/update-plan\b` -> `/plan-update-all` (1 occurrences)
- `/plan-update\b` -> `/plan-update-all` (1 occurrences)
