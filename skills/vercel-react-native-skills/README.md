# vercel-react-native-skills

**Tier**: beta (proven in CMT_Codebase, not yet reused elsewhere)

## Purpose

React Native and Expo best practices for building performant mobile apps. Use

## Installation

```
cp -r vercel-react-native-skills/ <project>/.claude/skills/
```

Then commit to the project's repo.

## Provenance

Ported from `~/.claude/skills/G-S-vercel-react-native-skills/` on 2026-04-22.

`G-S-` prefix stripped (was a global-skill marker; toolkit components don't need it).
