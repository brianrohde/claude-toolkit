# outcome_files

Outcome files document what actually happened when a plan was executed.

Filename matches the plan slug: `YYYY-MM-DD_<short-slug>.md`.

Sections: Completed / Adjusted / Dropped (omit if empty).
