# plan_files

Plans live here. Filenames follow the convention `YYYY-MM-DD_<kebab-slug>.md`.

Each plan should have YAML frontmatter with `created:` and `updated:` timestamps.

When a plan is executed, write a corresponding outcome file to `../outcome_files/`.
