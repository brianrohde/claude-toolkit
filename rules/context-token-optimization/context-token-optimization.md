---
paths:
  - "**"
---

# Token Optimization

**When to compact**: After exploration phase, after planning phase, before new chapter. Survives: CLAUDE.md, rules, memory, git state, tasks.

**Context hotspots** (read summaries only):
- Literature (49 papers) → summaries, never full
- Thesis sections (13 files) → load 1–2 per session
- System A/B agents → per-agent, not whole system

**Default model**: Haiku (cost-optimized)
**Upgrade to Sonnet**: Multi-file refactors, chapter editing
**Use Opus**: Architecture decisions, literature synthesis

**Tools**: Read (not cat), Grep (not rg), Glob (not find), Bash for system ops
